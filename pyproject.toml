[project]
name = "proxmox-pbs-dashboard"
version = "0.2.16"
description = "Read-only web dashboard for Proxmox VE virtual machines and PBS backups"
requires-python = ">=3.11"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"
